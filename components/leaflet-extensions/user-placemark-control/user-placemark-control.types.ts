export enum UserPlacemarkControlState {
    Free = 'free',
    Pan = 'pan',
    Centred = 'centred',
}

export type UserPlacemarkIcons = {
    buttonIcon: HTMLImageElement;
    buttonIconCentered: HTMLImageElement;
};
